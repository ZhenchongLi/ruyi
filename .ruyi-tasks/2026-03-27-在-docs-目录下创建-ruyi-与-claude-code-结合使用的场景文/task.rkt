(ruyi-task
  (goal "在 docs/ 目录下创建 ruyi 与 Claude Code 结合使用的中文场景文档，涵盖重构、审查、批量 issue、文档翻译、团队自动化、并行执行六大场景")
  (build ())
  (test ())
  (max-revisions 2)
  (min-score 7)
  (max-diff 400)
  (reviewer-model "sonnet")
  (auto-merge #t)
  (track #t)
  (forbidden ())
  (context ("CLAUDE.md" "RUYI-TASK-FORMAT.md" "README.md" "README.zh-CN.md" "evolve.rkt"))
  (judgement "文档应实用、结构清晰、命令示例可直接复制运行；语言自然流畅，避免机翻感；每个场景需包含：适用情境、完整命令、预期效果、注意事项")
  (subtasks
    ("创建 docs/scenarios.zh-CN.md 骨架：标题、目录、引言，简要说明 ruyi + Claude Code 的协作模式（双代理审查、worktree 隔离、可恢复执行），并列出六大场景的目录链接")
    ("编写场景一「日常开发中用 ruyi 做复杂重构」：包含典型重构目标示例、ruyi do 命令、task.rkt 配置要点（forbidden 保护关键文件、max-diff 控制变更粒度）、审查循环的工作流说明")
    ("编写场景二「用 ruyi 做代码审查和质量提升」：展示如何用高 min-score + 自定义 judgement 实现严格审查，包含 reviewer-model 选择建议、judgement 编写技巧、审查结果解读")
    ("编写场景三「用 ruyi 批量处理 GitHub issues」和场景四「用 ruyi 做文档和翻译任务」：issue 场景展示 ruyi do #123 和 pdo 批量处理多 issue 的用法；文档场景展示 build/test 设为空、context 引用源文件的配置方式")
    ("编写场景五「在 CLAUDE.md 中配置 ruyi 实现团队级自动化」：展示如何在项目 CLAUDE.md 中集成 ruyi 指令让团队成员直接使用，包含模板示例和最佳实践")
    ("编写场景六「用 pdo 并行执行多个独立任务」：详细说明 pdo 语法、worktree 隔离机制、适用场景（多模块独立修改、多语言翻译等），并给出完整的 pdo 命令示例和结果汇总说明")))
